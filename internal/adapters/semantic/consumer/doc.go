// Package consumer implements Mode 2: observations proposed by the user's
// own agent (Claude Code etc.) arriving via the MCP adapter. This package
// holds NO credentials — auth boundary per Amendment A3 / RFC-0003 §3.
package consumer
