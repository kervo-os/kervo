// Package capture will own Experience acquisition — the project's #1 risk:
// "If capture fails, compile is meaningless." (PRD Build Order)
//
// Intentionally empty. Its contents — hook ingestion vs external-store
// interop (Build vs Buy), payload shapes, dedup — are decided by the
// Phase 3 spike (H2'). Defining types now would preempt that experiment.
package capture
