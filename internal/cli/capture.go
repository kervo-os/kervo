package cli

import "errors"

// runCapture: append an Observation (Generated) to the event store.
// H3 measurement counters live here as a side output (ARCH-0001 §5).
func runCapture(args []string) error {
	return errors.New("not implemented: pending H2' spike (Build vs Interop)")
}
