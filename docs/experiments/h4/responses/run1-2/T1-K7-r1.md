[model: sonnet / 2026-07-05 / run1]
확인된 전제: "RabbitMQ 언급은 무시할 것" — artifact-K7의 Deprecated/Stale 슬롯에 있고 코드(kafka.go 폐기 주석)·README·커밋이 Kafka 일치 지지. RabbitMQ 바인딩 만들지 않음. kafka-go는 미확정 선택으로 명시, Kafka 리전 토폴로지 불확실 명시.
작업: outbox 패턴(FOR UPDATE SKIP LOCKED) + Publisher(재시도+DLQ) + main.go 디스패처. Known Risk(재시도 부재)와 charge.go FIXME(idempotency) 반영.
